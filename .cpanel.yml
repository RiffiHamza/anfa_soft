---
deployment:
  tasks:
    # The cPanel Node.js (Passenger) app's Application root points at this
    # repo. cPanel checks out the new commit here; touching tmp/restart.txt
    # makes Passenger reload and serve the updated ./out via server.js.
    - /bin/mkdir -p tmp
    - /bin/touch tmp/restart.txt
